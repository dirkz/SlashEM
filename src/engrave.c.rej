***************
*** 845,856 ****
  		break;
  
  	    case WEAPON_CLASS:
- #ifdef LIGHTSABERS
- 		if (is_lightsaber(otmp)) {
- 			if (otmp->lamplit) type = BURN;
- 			else Your("%s is deactivated!", aobjnam(otmp,"are"));
- 		} else
- #endif
  		if(is_blade(otmp)) {
  		    if ((int)otmp->spe > -3)
  			type = ENGRAVE;
--- 848,853 ----
  		break;
  
  	    case WEAPON_CLASS:
  		if(is_blade(otmp)) {
  		    if ((int)otmp->spe > -3)
  			type = ENGRAVE;
